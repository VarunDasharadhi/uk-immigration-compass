# 2026-09-24

## Archive indexing checkpoint

Authenticated Google Search Console evidence covers one affected URL, `/updates/archive`. Its
status is `Discovered - currently not indexed`, first detected on 5 September 2026, with last
crawled shown as N/A. Validation was already `Started` before this work.

The deployed raw response for `/updates/archive` was an empty generic SPA shell with homepage
metadata. Archive metadata and content appeared only after JavaScript ran. This is the main
technical risk to investigate, not a proven cause: last crawled N/A means Google had not yet made
a normal crawl judgement about the URL.

The architecture judgement selected route-specific prerender with full confidence. The freshness
hybrid had lower confidence, so the implementation uses the smallest build-time snapshot and
records its freshness ceiling.

## Changes and local evidence

The application changes cover routing and presentation in `App.tsx`, `NewsDashboard`,
`PageHero` and `UpdatesArchivePage`, and client metadata in `utils/seo.ts`. The build path uses
`index.html`, `package.json` and `scripts/generate-archive-prerender.mjs`. The new
`utils/seo.test.ts` and `scripts/generate-archive-prerender.test.ts` cover client metadata and the
static generator.

The local build generated `dist/updates/archive/index.html`. It contains archive-specific initial
metadata, one H1, up to 20 escaped static items, CollectionPage and ItemList JSON-LD when records
exist, and a no-items fallback that keeps the build successful. Sitemap freshness uses only a
build-bounded `createdAt` or a genuinely later `updatedAt`, with build time as the fallback. The
visible event date remains separate from that freshness value.

Security and failure-mode coverage includes HTML and JSON escaping, non-HTTP source URL rejection,
future source timestamps being ignored, hydration order matching, FAQ schema being absent outside
the homepage News view, graceful fallback for malformed or unavailable archive data, safe build
time handling for an invalid epoch, and unrelated sitemap entries remaining unchanged.

The complete local gate passed: 19 suites, 127 tests, type-check, lint, build, Node syntax check,
generator black-box tests and diff check.

## Scope and follow-ups

This work is local to `fix/archive-indexing`. Nothing has been pushed or deployed. No Search
Console request or validation action was performed.

These deliberate follow-ups remain outside this scope and were not fixed here: real routes for
other hash sections, branded 404 and soft-404 handling, a social share image, stable individual
update URLs, cache headers, and a possible content-order wording decision.

## Deployment and production verification

The local checkpoint above is now superseded by deployment. The reviewed archive prerender code
was committed as `cbd22d4` (`fix: prerender archive for search indexing`). The exact
`fix/archive-indexing` worktree at that commit was pushed as a fast-forward from remote `main`
`ac9248f` to remote `main` `cbd22d4`. GitHub Actions CI run `35948972615` for
`cbd22d4430431d22f6eb892a0f6762fff1a2d983` completed successfully.

Vercel production deployment `dpl_3faUA28ALsbpLGu6ZuLiYNKW54z5`, unique URL
`https://uk-immigration-compass-cs0cfhw5n-team-viper.vercel.app`, is Ready and aliased to
`https://uk-immigration-compass.vercel.app`. The production build completed and the archive
generator wrote 20 static items. This checkpoint makes no DNS or domain migration claim.

Raw production checks for `/updates/archive` and `/updates/archive/` both returned HTTP 200. Each
response has the archive title and description, `index, follow`, the exact self-canonical
`https://uk-immigration-compass.vercel.app/updates/archive`, one H1, 20 ItemList entries plus
static item markup, and a crawlable homepage link, with no FAQ schema. The Googlebot user-agent
with JavaScript disabled passed the same structural check. The raw JSON-LD parsed as a
`CollectionPage` with 20 `ItemList` entries.

The hydrated production page loaded 72 live API items at verification time, displayed 12 initially,
and expanded to 24 with Load more. Searching for `Prison Officer` returned one expected result,
the Work category filter worked, and the detail modal opened and closed. Canonical and robots
metadata remained correct. The hydrated metadata included the correct Open Graph and Twitter
values. There were no console errors, page errors, failed requests, or missing JavaScript or CSS
assets.

The production sitemap returned HTTP 200 with archive lastmod
`2026-09-24T01:46:06.237Z`. Robots returned HTTP 200 and allows all.

Search Console still shows the old `Discovered - currently not indexed` record with Last crawled
N/A immediately after deployment, which is expected before a recrawl. One Request Indexing attempt
ran the live test but failed with `Quota exceeded`; the interface asked to try tomorrow. `Validate
Fix` was started successfully on 24/09/2026, after which Search Console displayed `Validation
started`. At immediate verification, the URL still had Last crawled N/A and was not indexed. No
indexing or ranking outcome is claimed. Google can take days or weeks to recrawl and index.
